<div class="album py-5 bg-light">
  <div class="container">
    <%= link_to('Create new item', new_portfolio_path, class: 'btn btn-primary mb-4') if logged_in?(:site_admin) %>

    <div class="row">
      <%= render partial: 'portfolio_item', collection: @portfolio_items %>
    </div>
  </div>
</div>
