<span class="admin-action-icons">
  <%= link_to fa_icon('edit'), edit_path if logged_in?(:site_admin) %>
  <%= link_to fa_icon('trash'), object, method: :delete, data: { confirm: 'Are you sure you want to delete this post?' } if logged_in?(:site_admin) %>
</span>
